# CensusChat Documentation

The index lives in **[index.md](index.md)**. It routes by intent: run it,
understand the architecture, use the API, contribute.

This file used to carry a second, parallel catalog. Two indexes drift apart, so
there is now one.
